= simple_form_for [:administration, @category] do |f|
  = f.error_notification
  = f.input :name
  = f.input :image, as: :file
  = f.input :category_id, collection: Category.where(category_id: nil)

  = f.button :submit
