= simple_form_for [:administration, @page] do |f|
  = f.error_notification
  = f.input :title
  = f.input :slug
  = f.input :body, as: :text, input_html: { class: 'textarea' }
  = f.input :reserved_for, collection: Page.reserved_fors.keys

  = f.button :submit
